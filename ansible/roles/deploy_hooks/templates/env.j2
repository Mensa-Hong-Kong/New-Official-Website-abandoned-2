APP_NAME="Mensa"
APP_ENV={{ app.env }}
APP_KEY={{ app.key }}
APP_DEBUG=false
APP_URL={{ app.url }}
SESSION_LIFETIME=120

VITE_SESSION_LIFETIME="${SESSION_LIFETIME}"

# database (default)
DB_CONNECTION={{ database.mensa.type }}
DB_HOST={{ database.mensa.host }}
DB_PORT={{ database.mensa.port }}
DB_DATABASE={{ database.mensa.name }}
DB_USERNAME={{ database.mensa.user }}
DB_PASSWORD={{ database.mensa.password }}

OLD_DB_CONNECTION={{ database.old.type }}
OLD_DB_HOST={{ database.old.host }}
OLD_DB_PORT={{ database.old.port }}
OLD_DB_DATABASE={{ database.old.name }}
OLD_DB_USERNAME={{ database.old.user }}
OLD_DB_PASSWORD={{ database.old.password }}

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"
